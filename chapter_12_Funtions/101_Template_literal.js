// This is an example of a template literal in JavaScript. Template literals allow you to embed expressions inside string literals, using backticks (`) instead of single or double quotes.
function greet(name) {
    return `Hello, ${name}`;
}

let result =greet("Alice");
console.log(result);